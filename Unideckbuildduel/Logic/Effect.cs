namespace Unideckbuildduel.Logic
{
    /// <summary>
    /// The Effect enum is used to assign one effect to some cards.
    /// </summary>
    public enum Effect
    {
        noEffect,CanExchange, DrawOncePerTurn, DrawFromDiscardAtOnce, DrawFromDiscardOncePerTurn, OneMoreCard, PlayAgain, ProducesOne, Substitute
    }
}